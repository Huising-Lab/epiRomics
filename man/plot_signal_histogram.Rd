% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/epiRomics_visualizations.R
\name{plot_signal_histogram}
\alias{plot_signal_histogram}
\title{Plot signal distribution histogram for accessibility threshold selection}
\usage{
plot_signal_histogram(
  bw_paths,
  regions,
  n_bins = 100,
  show_z_lines = c(1, 1.5, 2),
  log_scale = TRUE,
  cex_label = 0.7,
  cex_title = 1
)
}
\arguments{
\item{bw_paths}{named character vector of BigWig file paths.}

\item{regions}{GRanges object of regions to examine.}

\item{n_bins}{integer, number of histogram bins (default: 100).}

\item{show_z_lines}{numeric vector, z-score thresholds to show as
vertical lines (default: c(1.0, 1.5, 2.0)).}

\item{log_scale}{logical, use log10(signal + 1) for x-axis (default: TRUE).}

\item{cex_label}{Numeric. Font size for threshold and annotation labels
(default 0.7).}

\item{cex_title}{Numeric. Font size for histogram titles (default 1.0).}
}
\value{
Invisible list of per-sample signal statistics, including
  mean, sd, quantiles, and suggested thresholds.
}
\description{
Visualizes the distribution of BigWig signal across genomic regions to
help identify the noise floor and select an appropriate z-score or raw
signal threshold for calling accessible regions. The distribution is
typically bimodal: a large peak at zero/low signal (background noise)
and a smaller peak at higher signal (accessible regions).
}
\examples{
tryCatch(plot_signal_histogram(c(Alpha = "nonexistent.bw"),
  GenomicRanges::GRanges()), error = function(e) message(e$message))
\donttest{
stats <- plot_signal_histogram(
  bw_paths = c(Alpha = "alpha.bw", Beta = "beta.bw"),
  regions = enhanceosome_regions
)
# Use stats to pick threshold:
stats$Alpha$suggested_threshold
}
}
