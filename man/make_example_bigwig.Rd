% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/synthetic-data.R
\name{make_example_bigwig}
\alias{make_example_bigwig}
\title{Create a temporary synthetic BigWig file for use in examples}
\usage{
make_example_bigwig(regions_gr = NULL, scores = NULL, path = NULL)
}
\arguments{
\item{regions_gr}{A \code{GRanges} object.  When \code{NULL} (default), a
small five-region GRanges on chr1 is used.}

\item{scores}{A numeric vector the same length as \code{regions_gr}.  When
\code{NULL} (default), scores 1--5 are used.}

\item{path}{Character file path for output.  When \code{NULL} (default), a
path is generated via \code{base::tempfile(fileext = ".bw")}.}
}
\value{
Character string giving the path to the created BigWig file.
}
\description{
Writes a BigWig file from synthetic \code{GRanges} and numeric scores.
The file is created in \code{base::tempdir()} by default.
}
\details{
\strong{Caller responsibility}: The returned path points to a temporary file
that persists for the R session. Call \code{file.remove(path)} when the
file is no longer needed to avoid accumulating temporary files.
}
\examples{
bw_path <- make_example_bigwig()
file.exists(bw_path)
file.remove(bw_path)
}
\seealso{
\code{\link{make_example_database}},
  \code{rtracklayer::export}

Other synthetic example data helpers: 
\code{\link{make_example_database}()},
\code{\link{make_example_enhanceosome}()},
\code{\link{make_example_putative_enhancers}()}
}
\concept{synthetic example data helpers}
