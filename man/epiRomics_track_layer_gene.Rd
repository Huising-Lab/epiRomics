% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/epiRomics_track_layer_fast.R
\name{epiRomics_track_layer_gene}
\alias{epiRomics_track_layer_gene}
\title{Visualize any gene locus with multi-track BigWig overlay}
\usage{
epiRomics_track_layer_gene(
  gene_symbol,
  epiRomics_dB,
  epiRomics_track_connection,
  chromatin_states = NULL,
  padding = 1000L,
  show_bigwig = TRUE,
  show_chromatin = TRUE,
  show_annotations = TRUE,
  show_gene_model = TRUE,
  show_enhancer_highlight = TRUE,
  mirror = TRUE,
  signal_style = c("line", "polygon"),
  signal_layout = "auto",
  cex_cell_label = 1.4,
  cex_axis = 1.2,
  cex_coord = 1.3,
  cex_annotation = 1.1,
  cex_gene = 1.2,
  cex_title = 1.5,
  cex_signal_label = 1.2,
  quantile_cap = 0.98,
  scale_factor = 1.1,
  export = NULL,
  width = 10,
  height = 8
)
}
\arguments{
\item{gene_symbol}{Character. HGNC gene symbol (e.g. \code{"INS"},
\code{"GCG"}, \code{"PDX1"}).}

\item{epiRomics_dB}{An epiRomics S4 database object.}

\item{epiRomics_track_connection}{A data.frame from the BigWig CSV
sheet (columns: path, name, color, type).}

\item{chromatin_states}{Optional. Output of
\code{\link{epiRomics_chromatin_states}}.}

\item{padding}{Integer. Base pairs of padding around the gene body
(default 1000).}

\item{show_bigwig}{Logical. Show BigWig signal tracks (default TRUE).}

\item{show_chromatin}{Logical. Show chromatin state tracks (default TRUE).}

\item{show_annotations}{Logical. Show BED annotation tracks (default TRUE).}

\item{show_gene_model}{Logical. Show gene model panel (default TRUE).}

\item{show_enhancer_highlight}{Logical. Show enhancer
highlight (default TRUE).}

\item{mirror}{Logical. Enable mirrored ATAC/RNA layout (default TRUE).}

\item{signal_style}{Character. Signal rendering style: \code{"line"}
(default) draws vertical bars at each position (IGV/UCSC browser style),
\code{"polygon"} draws filled area charts.}

\item{signal_layout}{Character. Signal layout mode: \code{"auto"},
\code{"stacked"}, or \code{"mirrored"}.}

\item{cex_cell_label}{Numeric. Font size for cell type labels (default 1.4).}

\item{cex_axis}{Numeric. Font size for axis labels (default 1.2).}

\item{cex_coord}{Numeric. Font size for coordinate bar (default 1.3).}

\item{cex_annotation}{Numeric. Font size for annotation labels (default 1.1).}

\item{cex_gene}{Numeric. Font size for gene model labels (default 1.2).}

\item{cex_title}{Numeric. Font size for plot title (default 1.5).}

\item{cex_signal_label}{Numeric. Font size for signal
indicators (default 1.2).}

\item{quantile_cap}{numeric. Percentile for capping
extreme signal peaks (default 0.98). Peaks above
this percentile are clipped to prevent axis
compression.}

\item{scale_factor}{numeric. Y-axis headroom
multiplier (default 1.1). Values above 1.0 add
whitespace above the tallest signal peak.}

\item{export}{Character or NULL. File path for
export (pdf, eps, png, tiff).}

\item{width}{Numeric. Export width in inches
(default 10).}

\item{height}{Numeric. Export height in inches
(default 8).}
}
\value{
Invisible NULL. A multi-panel base-R plot
  is drawn on the current graphics device.
}
\description{
A convenience wrapper around \code{\link{epiRomics_track_layer}} that
looks up a gene by symbol and creates a multi-track view without
requiring the full enhanceosome pipeline.
}
\details{
The function queries the TxDb for the official gene body coordinates,
builds a synthetic single-region epiRomics object, and passes it to
\code{epiRomics_track_layer} for rendering. The viewing window spans
the gene body plus \code{padding} on each side.
}
\examples{
db <- epiRomicsS4(annotations = GenomicRanges::GRanges(),
  meta = data.frame(name = character(), type = character(),
    file = character(), stringsAsFactors = FALSE),
  genome = "hg38")
tc <- data.frame(path = character(), name = character(),
  color = character(), type = character(),
  stringsAsFactors = FALSE)
tryCatch(epiRomics_track_layer_gene("INS", db, tc),
  error = function(e) message(e$message))
\donttest{
epiRomics_track_layer_gene("INS", epiRomics_dB,
  track_connection)
epiRomics_track_layer_gene("GCG", epiRomics_dB,
  track_connection, chromatin_states = cs,
  padding = 10000, export = "gcg_locus.pdf")
}
}
